$('#js-comment_errors').replaceWith("<%= escape_javascript(render(
    'comments/comment_errors'
  )) %>");
<% unless comment.errors.any? %>
	$('#comments').prepend("<%= escape_javascript(render(
      'comments/comment'
    )) %>");
<% end %>