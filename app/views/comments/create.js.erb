<% if comment.errors.any? %>
	$('.js-comment_error').removeClass('hidden');
<% else %>
	$('#comments').append("<%= escape_javascript(render(
      'comments/comment'
    )) %>");
	$('.js-comment_error').addClass('hidden');
<% end %>